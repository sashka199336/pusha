def test_function():# основная ф-ия, в которую определена вложенная ф-ия
    def inner_function():# вложенная ф-ия, печатающая сообщение
        print("Я в области видимости функции test_function")
    inner_function ()# вызов вложенной ф-ии работает
test_function()# вызов родительской ф-ии запускает вложенную

#inner_function()# Ошибка. Название ф-ии не определено.
